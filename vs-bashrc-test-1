# vs-bashrc-test-1
# This gets sourced from ~/.bashrc in testshell

alias lr='ls -lirt'
function vimsane {
    (
        export VimsaneCodeRoot=~lmatheson4/vimsane
        ~lmatheson4/vimsane/vimsane $*
    )
}

# vim: filetype=sh :
