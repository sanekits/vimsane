# vs-bashrc-test-1
# This gets sourced from ~/.bashrc in testshell

alias lr='ls -lirt'
PATH=$PATH:/bb/bin
function vimsane {
    (
        export VimsaneCodeRoot=~lmatheson4/vimsane
        ${VimsaneCodeRoot}/vimsane $*
    )
}

# vim: filetype=sh :
